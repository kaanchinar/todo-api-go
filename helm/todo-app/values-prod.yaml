# Production environment overrides

replicaCount: 3

image:
  tag: "sha-9e23d1f"

httpRoute:
  enabled: true
  hostnames:
    - todo.local

hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70

serviceMonitor:
  enabled: true
